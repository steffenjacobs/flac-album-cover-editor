[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
source = ["server", "scanner", "art_sources", "patcher"]
omit = ["tests/*", ".venv/*"]

[tool.coverage.report]
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
]
